=======================================================================
Recommended git hooks
=======================================================================

The scripts in git-hooks will run and update the deploy.sh script for
updating version.txt and making files readable/executable for anybody.

Install them like this:

cd .git/hooks
ln -snf ../../git-hooks/* .


